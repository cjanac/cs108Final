package finalWebProject.src.web;

import static org.junit.Assert.*;

import java.util.ArrayList;

import org.junit.*;


public class QuizTest {

	/*
	@Test
	public void arrayToStringTest() {
		ArrayList<String> list = new ArrayList<String>();
		String str = "\"";
		for(int i = 0; i < 2; i++){
			list.add("dane");
			str += "dane, ";
		}
		list.add("dane");
		str += "dane\"";
		assertEquals(str, Quiz.arrayToString(list));
	}
	
	
	@Test
	public void toArrayListTest() {
		String str = "blah, dane, blah, dane, hoohahs";
		ArrayList<String> list = new ArrayList<String>();
		list.add("blah");
		list.add("dane");
		list.add("blah");
		list.add("dane");
		list.add("hoohahs");
		assertEquals(list, Quiz.toArrayList(str));
	}
	
	@Test
	public void RandomGenerationTest() {
		ArrayList<String> list = new ArrayList<String>();
		for(int i = 0; i < 20; i++){
			list.add("dane" + i);
		}
		ArrayList<String> randlist = Quiz.generateRandomQSet(5, list);
		assertEquals(5, randlist.size());
		System.out.println(randlist.toString());
		randlist = Quiz.generateRandomQSet(20, list);
		assertEquals(20, randlist.size());
		System.out.println(randlist.toString());
		randlist = Quiz.generateRandomQSet(30, list);
		assertEquals(20, randlist.size());
		System.out.println(randlist.toString());
		randlist = Quiz.generateRandomQSet(2, list);
		assertEquals(2, randlist.size());
		System.out.println(randlist.toString());
	}
	*/
}
